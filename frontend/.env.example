# RPC Fuji: el browser usa por defecto /api/rpc/fuji (mismo origen); el servidor reenvía aquí.
# Opcional: RPC distinto al público (Infura, etc.) — solo servidor, no hace falta NEXT_PUBLIC.
# FUJI_RPC_UPSTREAM_URL=https://api.avax-test.network/ext/bc/C/rpc

# Si no querés proxy y preferís que el navegador llame directo al RPC (puede fallar por CORS):
# NEXT_PUBLIC_FUJI_RPC_DIRECT=1

# URL pública del nodo Fuji (upstream del proxy si FUJI_RPC_UPSTREAM_URL no está; opcional).
# No dejes la variable vacía (antes rompía el cliente).
NEXT_PUBLIC_AVALANCHE_FUJI_RPC=https://api.avax-test.network/ext/bc/C/rpc

# Contrato eERC20 desplegado en Fuji (EncryptedERC). Si vacío, usa demo Fuji del SDK.
NEXT_PUBLIC_EERC_CONTRACT_ADDRESS=

# Comprobar que el contrato responde en Fuji: npm run check:eerc

# ─────────────────────────────────────────────────────────────
# MODO DEL CONTRATO: standalone | converter
# ─────────────────────────────────────────────────────────────
# STANDALONE: El contrato crea un token eERC nuevo. Solo el owner puede mintear
#             saldo inicial a wallets (vía panel admin en /registro).
#             Ideal para demos controladas.
#
# CONVERTER:  El contrato envuelve un ERC20 existente. Cualquier wallet puede
#             depositar ERC20 y recibir eERC, o retirar eERC para recuperar ERC20.
#             Ideal para escenarios reales con tokens existentes.
# ─────────────────────────────────────────────────────────────
NEXT_PUBLIC_EERC_MODE=standalone

# Solo en modo converter: dirección del ERC20 subyacente (ej: USDC en Fuji)
NEXT_PUBLIC_CONVERTER_ERC20_ADDRESS=

# Bloque inicial para indexadores (futuro)
NEXT_PUBLIC_INDEXER_FROM_BLOCK=0

# (Opcional) Contrapartes demo pre-cargadas — se muestran junto a las instituciones
# registradas dinámicamente. Si están vacías, el directorio se carga solo desde la DB.
# NEXT_PUBLIC_DEMO_BANKAOOL=0x...
# NEXT_PUBLIC_DEMO_FINNOVA=0x...
# NEXT_PUBLIC_DEMO_RUTALOG=0x...

# Si está definida, Auditoría pide esta clave antes de mostrar el panel (demo pública)
NEXT_PUBLIC_AUDITOR_PREVIEW_SECRET=

# Neon PostgreSQL (solo servidor — nunca NEXT_PUBLIC)
# Pooled (app) + direct (migraciones drizzle-kit push)
DATABASE_URL=
DATABASE_URL_UNPOOLED=

# --- Demo “Cargar clave desde el deploy” (/api/demo/unlock-key) ---
# En Vercel: las TRES variables deben existir (sin NEXT_PUBLIC) y luego Redeploy.
# Valores = la frase que el equipo comparte + las claves de descifrado que generó
# register() para cada wallet demo (ver NEXT_PUBLIC_DEMO_* arriba).
# DEMO_TEAM_PASSPHRASE=
# DEMO_BANKAOOL_DECRYPTION_KEY=
# DEMO_FINNOVA_DECRYPTION_KEY=
