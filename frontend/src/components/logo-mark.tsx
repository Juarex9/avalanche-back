/** Wordmark tipográfico: Cello + punto (ver assets de marca). */
export function LogoMark() {
  return (
    <span className="logo-wordmark" aria-hidden="true">
      Cello
      <span className="logo-dot" />
    </span>
  );
}
